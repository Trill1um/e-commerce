# Backend Environment Variables
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=mern_db
DB_PORT=3306
PORT=3000

# For Docker, these are automatically set in docker-compose.yml
# You only need this file if running without Docker
